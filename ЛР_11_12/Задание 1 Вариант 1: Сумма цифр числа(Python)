def sum_of_digits(n):
    # Базовый случай: если число однозначное
    if n < 10:
        return n
    # Рекурсивный случай: последняя цифра + сумма остальных
    return n % 10 + sum_of_digits(n // 10)

# Тестирование
num = 1234
print(f"Сумма цифр числа {num} = {sum_of_digits(num)}")  # Вывод: 10
